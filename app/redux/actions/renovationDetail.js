import { createAction } from "redux-actions"
import { SET_RENOVATIONDETAIL, UPDATE_RENOVATIONDETAIL } from "../actionTypes"
export const setRenovationDetail = createAction(SET_RENOVATIONDETAIL)
export const updateRenovationDetail = createAction(UPDATE_RENOVATIONDETAIL)
